# Copyright 2025 Schelling Point Labs Inc
# SPDX-License-Identifier: AGPL-3.0-only

[package]
name = "stackable-hooks"
version = "0.1.0"
edition = "2021"
license = "BSD-2-Clause"
description = "Process-level interposition helpers with stackable hook dispatch"
build = "build.rs"

[features]
default = ["propagation-hooks"]
propagation-hooks = []
propagation-hooks-env-control = []

[dependencies]
ctor = "0.2"
libc = "0.2"
paste = "1.0"
