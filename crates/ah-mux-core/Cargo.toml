# Copyright 2025 Schelling Point Labs Inc
# SPDX-License-Identifier: AGPL-3.0-only

[package]
name = "ah-mux-core"
version = "0.1.0"
edition = "2021"
description = "Low-level, AH-agnostic multiplexer trait and shared types for terminal multiplexers"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
ah-test-utils = { workspace = true }
