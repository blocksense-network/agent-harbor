name: testing_workflow_scenario
repo:
  init: true
timeline:
  - think:
      - [500, "Processing request for testing_workflow_scenario"]
  - agentToolUse:
      toolName: "write_file"
      args:
        path: "test.txt"
        text: "Test content for testing_workflow_scenario"
      result: "File created"
      status: "ok"
  - assistant:
      - [300, "Completed testing_workflow_scenario scenario"]
expect:
  exitCode: 0
  artifacts:
    - type: "taskFile"
      pattern: "test.txt"
