# Copyright 2025 Schelling Point Labs Inc
# SPDX-License-Identifier: AGPL-3.0-only

name: feature_implementation_scenario
repo:
  init: true
timeline:
  - think:
      - [500, "Processing request for feature_implementation_scenario"]
  - agentToolUse:
      toolName: "writeFile"
      args:
        path: "test.txt"
        text: "Test content for feature_implementation_scenario"
      result: "File created"
      status: "ok"
  - assistant:
      - [300, "Completed feature_implementation_scenario scenario"]
expect:
  exitCode: 0
  artifacts:
    - type: "taskFile"
      pattern: "test.txt"
