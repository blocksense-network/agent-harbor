name: basic_timeline_scenario
repo:
  init: true
timeline:
  - think:
      - [500, "Processing request for basic_timeline_scenario"]
  - agentToolUse:
      toolName: "write_file"
      args:
        path: "test.py"
        text: "print('Hello, timeline!')"
      result: "File created"
      status: "ok"
  - assistant:
      - [300, "Completed basic_timeline_scenario scenario"]
expect:
  exitCode: 0
  artifacts:
    - type: "taskFile"
      pattern: "test.py"
