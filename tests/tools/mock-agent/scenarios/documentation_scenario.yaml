name: documentation_scenario
repo:
  init: true
timeline:
  - think:
      - [500, "Processing request for documentation_scenario"]
  - agentToolUse:
      toolName: "writeFile"
      args:
        path: "test.txt"
        text: "Test content for documentation_scenario"
      result: "File created"
      status: "ok"
  - assistant:
      - [300, "Completed documentation_scenario scenario"]
expect:
  exitCode: 0
  artifacts:
    - type: "taskFile"
      pattern: "test.txt"
